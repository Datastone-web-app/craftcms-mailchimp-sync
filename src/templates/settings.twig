{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
  first: true,
  label: 'Api key',
  name: 'apiKey',
  instructions: 'Go to account & billing, Extras, API keys',
  value: settings.apiKey
}) }}

{{ forms.textField({
  label: 'Audience id',
  name: 'listId',
  instructions: 'Go to Audience, All contacts, Settings, Audience name & defaults',
  value: settings.listId
}) }}